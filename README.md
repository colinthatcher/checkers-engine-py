# checkers-engine-py

## Install
`uv venv`
`source .venv/bin/activate`
`uv sync`

## Run API Server
`fastapi dev main.py`

## Run Tests
`pytest`

## Run Tests (Verbose)
`pytest -s`

## Run Specific Test (Verbose)
`pytest -s -k "test_attempt_move_dev"`

## Setup Debugging
TODO